{
  "name": "@repo/api-client",
  "private": true,
  "type": "module",
  "exports": {
    "./lgg": "./src/lgg/index.ts"
  },
  "scripts": {
    "lint": "eslint .",
    "format": "eslint . --fix",
    "check-types": "tsc --noEmit",
    "codegen:lgg": "rm -rf src/lgg/generated && openapi-generator-cli generate -i http://localhost:8000/openapi.json -g typescript-fetch -o src/lgg/generated --additional-properties=disallowAdditionalPropertiesIfNotPresent=false,supportsES6=true,useSingleRequestParameter=false --enable-post-process-file --remove-operation-id-prefix",
    "codegen": "pnpm codegen:lgg"
  },
  "devDependencies": {
    "@openapitools/openapi-generator-cli": "^2.23.1",
    "@repo/config": "workspace:*",
    "eslint": "^9.37.0"
  }
}
